
server:
  host: localhost
  port: 8080
  read_timeout: 30s
  write_timeout: 30s
  max_connections: 100
  ssl: false

database:
  driver: postgres
  host: db.localhost
  port: 5432
  username: admin
  password: securepassword
  name: dev_db
  ssl_mode: require

logging:
  level: info
  format: json
  file: app.log

features:
  caching: true
  monitoring: false
  analytics: true
