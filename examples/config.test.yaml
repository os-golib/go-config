server:
  host: localhost
  port: 8080
  read_timeout: 30s
  write_timeout: 30s
  max_connections: 100
  ssl: false

database:
  driver: postgres
  host: "localhost"
  port: 5432
  username: admin
  password: securepassword
  name: "test_db"
  ssl_mode: require

logging:
  level: "debug"
  format: "text"

features:
  caching: false
  monitoring: true
  debug_mode: true
  